# Gulp Template for quick start simple web projects

## Install gulp-cli first

`npm install --global gulp-cli`

## Then goto project dir & install npm packages

`
cd [project_dir]
npm install
`
## Start gulp

`npm start`
